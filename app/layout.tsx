import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Portfolio | Data Scientist & ML Engineer",
  description: "Data Scientist and Machine Learning Engineer specializing in building intelligent solutions with Python, TensorFlow, and PyTorch.",
  keywords: ["Data Scientist", "Machine Learning", "ML Engineer", "Python", "TensorFlow", "PyTorch", "AI"],
  authors: [{ name: "[Your Name]" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
